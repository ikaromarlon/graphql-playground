{
  "name": "graphql-playground",
  "version": "0.0.1",
  "description": "Just a playground to learn and practice with Graphql",
  "license": "UNLICENSED",
  "type": "module",
  "engines": {
    "node": "^20.0.0"
  },
  "scripts": {
    "start": "node --env-file=.env src/main.js",
    "dev": "nodemon",
    "lint": "standard --fix"
  },
  "dependencies": {
    "@apollo/server": "^4.10.0",
    "graphql": "^16.8.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.3",
    "standard": "^17.1.0"
  },
  "standard": {
    "env": [
      "node"
    ]
  }
}
